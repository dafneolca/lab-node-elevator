"use strict";

class Person {
  constructor(name, originFloor, destinationFloor){

    this.name=name;
    this.originFloor=originFloor;
    this.destinationFloor=destinationFloor;

// Now, we need to think about what happens when a person ‘calls’ the elevator.
// In the Elevator class, it's time to write the code for the call method.
// This method should receive a person object and add it as a request into the elevator’s queue.

// CALL ELEVATOR CLASS
  }
}

let Dafne = new Person ("Dafne", 0, 4);

module.exports = Person;
